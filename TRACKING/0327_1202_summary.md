# 阶段总结 2026-03-27 12:02

## 变更清单

### 新增文件

| 文件 | 内容 |
|------|------|
| `BSP/Inc/bsp_i2c1.h` | I2C1 硬件宏（PB6/PB7，AF4，400kHz）+ API 声明 + `g_motorIcAddr`/`g_motorIcName` extern |
| `BSP/Src/bsp_i2c1.c` | Init、RecoverBus、WriteReg、ReadReg、ReadRegs、Scan 完整实现 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `Core/Inc/stm32f4xx_conf.h` | 新增 `#include "stm32f4xx_i2c.h"` |
| `Core/Src/main.c` | 引入 `bsp_i2c1.h`，初始化序列加入 `BSP_I2C1_Init()`，失败时 `while(1)` 挂起；`HEARTBEAT_INTERVAL_MS` 调回 500ms |
| `Makefile` | 追加 `BSP/Src/bsp_i2c1.c` 和 `stm32f4xx_i2c.c` |
| `CLAUDE.md` | 外设表 `BSP_I2C_` → `BSP_I2C1_`，新增 I2C3（RT5112WSC）行；I2C 模块约定拆分为 I2C1/I2C3 两节；路线图 3.5 补充 PMIC 型号 |

### Code Review 修复

| 问题 | 修复位置 | 内容 |
|------|----------|------|
| WritePhase 未检测 AF 标志 | `bsp_i2c1.c:WritePhase` | 地址 NACK 时立即退出，不等满超时 |
| Init 返回值被忽略 | `main.c` | 失败时 `while(1)` 挂起，便于调试发现 |

---

## 当前系统状态

### 路线图进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | CLAUDE.md 规则确定 | **完成** |
| 1 | Makefile + 启动文件 + LED 闪烁 | **完成** |
| 2 | UART 收发（环形缓冲 + DMA 发送） | **完成** |
| 3 | I2C1 驱动 + Scan | **完成，板上验证通过** |
| 3.2 | 协议格式确定 | **完成** |
| 3.4 | 串口指令解析实现 | **完成，已验证** |
| 3.5 | PMIC 配置（RT5112WSC，I2C3） | 待开始（寄存器配置待确认） |
| 4 | 完整协议 + 采样上报 | 未开始 |

### 各模块完成度

| 模块 | 状态 | 备注 |
|------|------|------|
| BSP LED | 完成 | 500ms 心跳 |
| BSP UART | 完成 | 环形缓冲接收 + DMA 发送 |
| BSP I2C1 | 完成，板上验证 | Init/WriteReg/ReadReg/ReadRegs/Scan/RecoverBus |
| BSP I2C3 | 未开始 | PMIC，等寄存器配置确认 |
| BSP TIM | 框架完成 | 采样定时器待启用 |
| App Protocol | 完成并验证 | 心跳 + 错误响应；业务命令待扩展 |
| App Motor | 未开始 | 依赖协议命令扩展 |
| App Sample | 未开始 | 依赖采样定时器 + 协议扩展 |

### 已知遗留问题

- I2C Scan 命令码尚未在协议层定义，无法从上位机触发
- PMIC RT5112WSC 通道分配和寄存器初始化序列待确认
- 业务命令（`0x01~0x7F`）尚未实现

### 下一步选项

1. **扩展协议命令**：定义 Scan 命令码，从上位机触发 I2C 扫描验证设备在线
2. **推进阶段 3.5**：待 PMIC 寄存器配置确认后实现 I2C3 驱动 + PMIC 初始化
