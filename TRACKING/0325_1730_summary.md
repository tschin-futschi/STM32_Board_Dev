# 总结 2026-03-25 17:30

## 变更清单

### 修改文件

| 文件 | 改动 |
|------|------|
| `BSP/Inc/bsp_led.h` | GPIO 端口 GPIOG → **GPIOF**，引脚 PG13 → **PF13**；删除 LED2（PG14）相关宏和函数声明 |
| `BSP/Src/bsp_led.c` | 删除 LED2 全部函数，仅保留 LED1（PF13） |
| `Core/Src/main.c` | 诊断验证通过后，恢复 SysTick 500ms 心跳模式（待实测） |
| `Makefile` | ① TMPDIR/TMP/TEMP 移到 BUILD_DIR 定义之后，修复 `:=` 立即求值为空的 bug；② flash 目标：`stlink-v2.cfg` → `stlink.cfg`，`.bin` + 地址偏移 → `.elf`（自带地址） |
| `CLAUDE.md` | 工作流程新增第 2 条：修改代码前必须说明要改什么文件、达到什么目的 |

### 新增文件

| 文件 | 说明 |
|------|------|
| `TRACKING/0325_1233_summary.md` | 上次对话遗留的总结文件 |
| `TRACKING/0325_1657_summary.md` | 诊断阶段总结 |

## 当前系统状态

### 路线图进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | CLAUDE.md 规则确定 | **完成** |
| 1 | Makefile + 启动文件 + LED 闪烁 | **进行中** — 软件延时诊断通过（LED 闪烁），SysTick 模式待实测 |
| 2 | UART 收发 | 未开始 |
| 3 | I2C 驱动 + Scan | 未开始 |
| 4 | 协议框架 | 未开始 |
| 5 | 完整协议 + 采样上报 | 未开始 |

### 已确认事项

- 板子外部晶振 **8 MHz**，PLL_M=8 配置正确
- LED 在 **PF13**，高电平点亮，仅 1 个 LED
- 软件延时诊断通过：GPIO 和烧录流程正常

### 待验证

1. SysTick 模式下 LED 是否正常 500ms 闪烁（已编译，待烧录）
2. `make flash`（改用 `stlink.cfg` + `.elf`）是否稳定
3. CLAUDE.md 中 LED 引脚描述（PG13/PG14 双灯）需更新为 PF13 单灯
