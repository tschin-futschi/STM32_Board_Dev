# 总结 0328_1205

## 变更清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `BSP/Inc/bsp_i2c2.h` | I2C2 硬件配置宏（PB10/PB11，AF4，400 kHz）+ `g_motorIcAddr` / `g_motorIcName` extern 声明 |
| `BSP/Src/bsp_i2c2.c` | I2C2 完整驱动：Init / WriteReg / ReadReg / ReadRegs / Scan / RecoverBus |

### 修改文件

| 文件 | 具体改动 |
|------|----------|
| `BSP/Inc/bsp_i2c1.h` | 移除 Motor IC 运行时配置（`g_motorIcAddr` / `g_motorIcName` extern），注释改为 INA 功耗/电流测量 |
| `BSP/Src/bsp_i2c1.c` | 移除 `g_motorIcAddr` / `g_motorIcName` 定义，注释改为 INA |
| `Core/Src/main.c` | 新增 `#include "bsp_i2c2.h"`；初始化序列加入 `BSP_I2C2_Init()`；I2C1 注释改为 INA；`HEARTBEAT_INTERVAL_MS` 100→500 ms |
| `Makefile` | 追加 `BSP/Src/bsp_i2c2.c` |
| `CLAUDE.md` | 外设引脚配置表更正：I2C1→INA，新增 I2C2（PB10/PB11）→电机 IC，所有 I2C 标注主机模式；I2C 模块实现约定同步更新；`g_motorIcAddr` 声明位置改为 bsp_i2c2 |
| `Test/Src/test_pmic.c` | （已有未提交改动）功能改为轮询读取 PMIC 寄存器 0x00~0x14，每 100 ms 读一个，循环上报 |

### 硬件事实确认（本次会话）

| I2C | 引脚 | 器件 | 用途 |
|-----|------|------|------|
| I2C1 | PB6/PB7，AF4 | INA | 测量板子功耗和电流 |
| I2C2 | PB10/PB11，AF4 | 电机 IC | 电机寄存器读写（原理图确认） |
| I2C3 | PA8/PC9，AF4 | RT5112WSC | PMIC |

- 所有 I2C 均为主机模式（Master）

## 当前系统状态

### 路线图进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | CLAUDE.md 规则确定 | 完成 |
| 1 | Makefile + 启动文件 + LED 闪烁 | 完成 |
| 2 | UART 收发（环形缓冲 + DMA 发送） | 完成 |
| 3 | I2C 驱动 + Scan | **进行中**（bsp_i2c2 已建，Scan 待实现） |
| 3.2 | 协议格式确定 | 完成 |
| 3.4 | 串口指令解析 | 未开始 |
| 3.5 | PMIC 配置 | 待开始 |
| 4 | 完整协议 + 采样上报 | 未开始 |

### 已知遗留问题

- `Test/Src/test_pmic.c` 改动尚未提交
- I2C Scan 验证测试（三路）尚未实现
