# 工作总结 — 2026-03-25 12:20

## 变更清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `Makefile` | 构建系统，含 CPU 标志、SPL 路径、编译/链接/烧录规则，修复了 Windows TEMP 权限问题 |
| `Linker/STM32F429ZGTX_FLASH.ld` | 链接脚本：Flash 1MB、SRAM1 112KB、CCM 64KB，栈 2KB、堆 0 |
| `Core/Startup/startup_stm32f429_439xx.s` | 从 SPL（SW4STM32 模板）复制的 GCC 启动文件 |
| `Core/Src/system_stm32f4xx.c` | 从 SPL 模板复制，**PLL_M 改为 8**（适配 HSE 8MHz → 180MHz） |
| `Core/Inc/stm32f4xx_conf.h` | SPL 配置，阶段 1 启用 rcc、gpio、misc |
| `Core/Inc/main.h` | 主程序头文件 |
| `Core/Inc/stm32f4xx_it.h` | 中断处理函数声明 |
| `Core/Src/stm32f4xx_it.c` | 中断处理实现，SysTick_Handler 调用 BSP_Tick_Increment |
| `Core/Src/main.c` | 主程序：NVIC 分组 → SysTick → LED Init → 500ms 心跳循环 |
| `BSP/Inc/bsp_tick.h` | SysTick 毫秒计数器接口 |
| `BSP/Src/bsp_tick.c` | SysTick 毫秒计数器实现 |
| `BSP/Inc/bsp_led.h` | LED 驱动接口，PG13/PG14 宏定义 |
| `BSP/Src/bsp_led.c` | LED 驱动实现，Init/Toggle/On/Off |

### 新增目录

`Core/Inc/`、`Core/Src/`、`Core/Startup/`、`BSP/Inc/`、`BSP/Src/`、`App/Inc/`、`App/Src/`、`Linker/`

### 环境变更

- 通过 MSYS2 pacman 安装了 `mingw-w64-x86_64-arm-none-eabi-gcc` 13.3.0 和 `arm-none-eabi-newlib`
- 编译需要 PATH 包含 `/c/msys64/mingw64/bin`，make 使用 `/c/msys64/usr/bin/make`

### 修改文件

| 文件 | 改动 |
|------|------|
| `CLAUDE.md` | 新增"单功能逐个实现"强制约束（工作流程第 2 条）和"复用原则"小节 |
| `Core/Src/system_stm32f4xx.c` | PLL_M 从 25 改为 8（HSE 8MHz 适配） |

### 构建结果

```
text    data    bss     dec     hex     filename
1456    12      2084    3552    de0     build/firmware.elf
```

我方代码零 warning、零 error。所有 warning 来自 SPL 库头文件（DBGMCU 宏重定义）和 newlib 存根。

---

## 当前系统状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | CLAUDE.md 规则确定 | **完成** |
| 1 | Makefile + 启动文件 + LED 闪烁 | **编译通过，待烧录验证** |
| 2 | UART 收发 | 未开始 |
| 3 | I2C 驱动 + Scan | 未开始 |
| 4 | 协议框架 | 未开始 |
| 5 | 完整协议 + 采样上报 | 未开始 |

- **编译验证**：`make DEBUG=1` 通过，生成 `build/firmware.bin`
- **烧录验证**：需连接 ST-Link 后执行 `make flash`，确认 LED PG13 以 500ms 间隔闪烁
- **已知问题**：
  - Windows 环境下 GCC 临时文件权限问题，已通过 Makefile 中 export TMP/TEMP/TMPDIR 到 build 目录解决
  - 编译需手动设置 PATH（`/c/msys64/mingw64/bin`），后续可考虑在 CLAUDE.md 中记录
